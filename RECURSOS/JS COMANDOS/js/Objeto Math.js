// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.1); // Ceil siempre redondea hacia arriba
resultado = Math.floor(2.9); // Floor siempre redondea hacia abajo
resultado = Math.sqrt(144); // raiz cuadrada
resultado = Math.abs(-200); //lo convierte en positivo
resultado = Math.min( 3, 5, 1, 8 , 2, 10 ); // muestra el valor minimo
resultado = Math.max( 3, 5, 1, 8 , 2, 10 );// muestra el valor aximo
resultado = Math.random(); //muestra numero aleatorio pero menor a uno en decimal
resultado = Math.floor( Math.random() * 30); //numero aleatorio hasta el 30

console.log(resultado);


//para ver todos los comandos y funciones de JS escribimos en la consola del navegador window y desplegamos el menu